import EventForm from '../../src/components/EventForm'

// Create mode. The shared form handles everything; edit mode lives in [id].tsx.
export default function NewEventScreen() {
  return <EventForm />
}
